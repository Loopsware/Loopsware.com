
<form 
    class="border-3 border-gray-200 rounded-2xl p-6 lg:p-8"
    action="https://formspree.io/f/xpzvlkov"
    method="POST"
>
    <div class="flex">
        <input type="text"  class="h-14 w-full bg-gray-100 rounded-xl my-2 p-6" placeholder="Your name" />
    </div>
    <div class="flex">
        <input type="email" class="h-14 w-full bg-gray-100 rounded-xl my-2 p-6" placeholder="Your email" />
    </div>
    <div class="flex">
        <textarea name="message" class="w-full h-56 bg-gray-100 rounded-xl resize-none my-2 p-6" placeholder="A short description about your next app idea..." />
    </div>
    <div class="flex flex-col md:flex-row justify-between items-center mt-4">
        <p>Or contact us: <a class="text-blue-400" href="mailto:hello@loophau5.com">hello@loopsware.com</a></p>
        <button type="submit" class="w-full md:w-6/12 lg:w-4/12 xl:w-3/12 bg-stone-900 rounded-full p-4 mt-5 md:mt-0">
            <p class="text-white font-bold">Send</p>
        </button>
    </div>
</form>

<script type="module">
import anime from 'https://cdn.skypack.dev/animejs';

// New code
const contactForm = document.querySelector('.contact-form');

anime({
    targets: contactForm,
    opacity: [0, 1], // Fade the component in
    translateY: [250, 0], // Move the component up
    duration: 2000,
    delay: 1000, // Delay the animation by 500ms
    easing: 'easeOutExpo' // Use an exponential easing function
});
</script>